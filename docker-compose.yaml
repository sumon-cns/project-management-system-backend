version: '3.9'

services:
  postgres:
    image: postgres:16.3-alpine3.20
    ports:
      - "5432:5432"
    # volumes:
    #   - ~/apps/postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=123
      - POSTGRES_DB=project_management_system_db1
